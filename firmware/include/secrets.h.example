#pragma once

// Copy this file to secrets.h and fill in your network and backend.

#define WIFI_SSID "your-ssid"
#define WIFI_PASS "your-password"

// Hostname or IP of the machine running the FastAPI backend (same LAN as the ESP).
#define BACKEND_HOST "192.168.1.100"
