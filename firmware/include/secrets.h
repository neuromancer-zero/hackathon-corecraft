#pragma once

// Placeholder values so the project compiles out of the box.
// For real use, copy secrets.h.example to secrets.h and edit.

#define WIFI_SSID "ssid"
#define WIFI_PASS "pass"
#define BACKEND_HOST "192.168.1.100"
